<UserControl x:Class="EntityEditor.Controls.PrefabTile"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:entityEditor="clr-namespace:EntityEditor"
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="50">
    <Grid Width="50" Height="50" Margin="5"
          d:DataContext="{d:DesignInstance entityEditor:PrefabTileData}">
        <Border Background="{Binding Path=Color}">
            <Grid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
