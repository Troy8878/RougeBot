<UserControl x:Class="EntityEditor.Entities.Representations.ComponentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:project="clr-namespace:EntityEditor.Entities.Project"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid d:DataContext="{d:DesignInstance project:Component}" TextBlock.FontSize="20">
        <ItemsControl ItemsSource="{Binding Path=Properties}" Margin="10,5,5,5">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel Margin="2" Orientation="Horizontal">
                        <TextBlock Text="{Binding Path=Key}" Margin="0,0,5,0" Width="150" />
                        <Border DataContext="{Binding Path=Value}">
                            <ContentPresenter Content="{Binding}" ContentTemplate="{Binding Path=RenderTemplate}" />
                        </Border>
                    </StackPanel>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>
