<Window x:Class="EntityEditor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="480" Width="800"
        Background="Black"
        Foreground="SpringGreen">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <Button Grid.Row="0" Grid.Column="0" Width="200" Height="75"
                FontSize="30" Click="ButtonPrefab">
            Prefab Editor
        </Button>
        <Button Grid.Row="0" Grid.Column="1" Width="200" Height="75"
                FontSize="30" Click="ButtonEntity">
            Entity Editor
        </Button>

        <StackPanel Grid.Row="2" Grid.Column="0">
            <Button Margin="10" Width="200" Height="35"
                    FontSize="20" Click="ChangeDirClick">
                Change Repo Dir</Button>
            <TextBlock x:Name="RepoDirDisplay" TextWrapping="Wrap"
                       Margin="20" HorizontalAlignment="Center" />
        </StackPanel>
    </Grid>
</Window>