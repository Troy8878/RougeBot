<Window x:Class="EntityEditor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:EntityEditor.Views"
        Title="MainWindow" Height="480" Width="800"
        Background="{StaticResource RbBackground}"
        Foreground="{StaticResource RbText}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Width="200" Height="75"
                FontSize="30" Click="ButtonPrefab">
                Prefab Editor
            </Button>
            <Button Grid.Column="1" Width="200" Height="75"
                FontSize="30" Click="ButtonEntity">
                Entity Editor
            </Button>
        </Grid>
        
        <TextBlock Grid.Row="1" Margin="50,0" FontSize="18" TextWrapping="Wrap">
            WARNING! All Changes are made immediately!
            Remember to use Git to backup all changes you
            make! You can always commit and push later!
        </TextBlock>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            
            <Button HorizontalAlignment="Center" Grid.Column="0"
                    VerticalAlignment="Top" Padding="30,10" Click="CommitClick">
                Commit
            </Button>
            
            <views:GitView Grid.Column="1" x:Name="GitView" />

            <Button HorizontalAlignment="Center" Grid.Column="2"
                    VerticalAlignment="Top" Padding="30,10" Click="SyncClick">
                Sync
            </Button>
            
        </Grid>
        
        <StackPanel Grid.Row="3">
            <Button Margin="10" Width="200" Height="35"
                    FontSize="20" Click="ChangeDirClick">
                Change Repo Dir</Button>
            <TextBlock x:Name="RepoDirDisplay" TextWrapping="Wrap"
                       Margin="20" HorizontalAlignment="Center" />
        </StackPanel>
    </Grid>
</Window>